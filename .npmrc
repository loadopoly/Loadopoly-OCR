# Allow optional peer dependencies to fail gracefully
# This is needed because @react-three/fiber has optional Expo/React Native peer deps
# that we don't need for web-only builds
legacy-peer-deps=true
