name: Deploy Marketing Site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy app screenshots and icons into website folder
        run: |
          cp public/screenshot-desktop-1.png loadopoly-website/ 2>/dev/null || true
          cp public/screenshot-desktop-2.png loadopoly-website/ 2>/dev/null || true
          cp public/screenshot-desktop-3.png loadopoly-website/ 2>/dev/null || true
          cp public/screenshot-mobile-1.png  loadopoly-website/ 2>/dev/null || true
          cp public/screenshot-mobile-2.png  loadopoly-website/ 2>/dev/null || true
          cp public/icon.svg                 loadopoly-website/ 2>/dev/null || true
          cp public/icon-192.png             loadopoly-website/ 2>/dev/null || true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: loadopoly-website

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
